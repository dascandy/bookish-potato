#pragma once

#include <vector>
#include <utility>

namespace s2 {

template <typename K, typename V>
class flatmap : private vector<pair<const K, V>> {
public:
  typedef typename vector<pair<const K, V>>::iterator iterator;
  typedef typename vector<pair<const K, V>>::const_iterator const_iterator;
  using vector<pair<const K, V>>::begin;
  using vector<pair<const K, V>>::end;
  using vector<pair<const K, V>>::size;
  V &operator[](const K &key) {
    for (pair<const K, V> &p : *this) {
      if (p.first == key) return p.second;
    }
    this->push_back(pair<const K, V>(key, V()));
    return this->back().second;
  }
  void emplace(K k, V v) {
    (*this)[move(k)] = move(v);
  }
  template <typename P>
  void insert(const P & p) {
    (*this)[p.first] = p.second;
  }
  iterator find(const K &k) {
    auto it = this->begin();
    while (it != this->end()) {
      if (it->first == k) break;
      ++it;
    }
    return it;
  }
  const_iterator find(const K &k) const {
    auto it = this->begin();
    while (it != this->end()) {
      if (it->first == k) break;
      ++it;
    }
    return it;
  }
};

}


